# Redis connection - required for both local development and production
REDIS_URL=redis://localhost:6379

# Node environment - development/production
NODE_ENV=development

# Server port - optional, defaults to 3000
PORT=3000